server:
  port: 8080
  servlet:
    context-path: /api/v1/ecsvolume/write

management:
  server:
    port: 8081
  endpoints:
    web:
      exposure:
        include: health, info, metrics

spring:
  application:
    name: ECS-VOLUME-WRITE

app:
  file:
    path: /data/write/task.log